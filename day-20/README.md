# Day 20 - Bash Script Task

## Objective

My goal today is to have a look and try working on a Bash Task

---

## What I Learned

While looking through the excercise the first task was to create a python virtual environment. While I have heard of virtual environments, I really do not undertsand how it works, so I had to take time to learn and know it

#### Virtual Environments
A virtual environment is an isolated space that has its own  settings and dependencies (libraries, tools, or configurations) and doesn’t interfere with other projects.

It is like having separate lockers:
- Each locker can store different versions of the same items
- What you do in one locker doesn’t affect the others

#### Python Virtual Environment
A python virtual environment is created with the venv python module.

A python virtual environment is:
- Used to contain a specific Python interpreter and software libraries and binaries which are needed to support a project. These are by default isolated from software in other virtual environments and Python interpreters and libraries installed in the operating system.
- Contained in a directory, conventionally named .venv or venv in the project directory, or under a container directory for lots of virtual environments, such as ~/.virtualenvs
- Is not checked into source control systems such as Git.
- Considered as disposable – it should be simple to delete and recreate it from scratch. No project code should be placed in the environment.

Python virtual environments are created by executing the venv module:
`python -m venv /path/to/new/virtual/environment`

---

## What I Built / Practiced

-  I learnt about virtual environment and refreshed some Linux concepts 

---

## Challenges Faced

- none

---

## Key Takeaways

- A virtual environment is used in many areas (not just Python), like virtual machines or containers.
- pratice cements knowledge

---

## Resources

- https://github.com/Najeeb-Sulaiman/linux-and-bash-scripting-guide/blob/main/08-tasks/task.md
- https://docs.python.org/3/library/venv.html 

---
